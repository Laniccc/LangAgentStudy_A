# Conda 环境：LangAgent（项目 LangAgent_A）
# 创建：conda env create -f environment.yml
# 更新：conda env update -f environment.yml --prune

name: LangAgent
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - -r requirements.txt
